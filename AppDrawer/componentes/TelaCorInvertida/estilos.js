import { StyleSheet } from "react-native";

const estilosInvertida = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
    backgroundColor: "#000",
  },

  texto: {
    fontSize: 22,
    color: "#00ff00",
  },
});

export default estilosInvertida
